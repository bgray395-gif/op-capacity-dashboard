services:
  - type: web
    name: op-capacity-dashboard
    env: docker
    plan: starter
    healthCheckPath: /health
    autoDeploy: true
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1
